package nl.gymlog.ui.theme

import androidx.compose.ui.graphics.Color

// ── Background system ──────────────────────────────────────────────────────────
val Background = Color(0xFF0D0D0D)
val SurfaceCard = Color(0xFF1A1A1A)
val Border = Color(0xFF2C2C2C)

// ── Text ───────────────────────────────────────────────────────────────────────
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFF8A8A8A)
val TextMuted = Color(0xFF4A4A4A)

// ── Status ─────────────────────────────────────────────────────────────────────
val AmberWarning = Color(0xFFFFB300)

// ── Metric accent colors ───────────────────────────────────────────────────────
val CaloriesColor = Color(0xFFFF6B35)       // orange
val DurationColor = Color(0xFF4ECDC4)       // teal
val DistanceColor = Color(0xFF45B7D1)       // sky blue
val AvgPowerColor = Color(0xFF96CEB4)       // sage
val AvgSpeedColor = Color(0xFFFFEAA7)       // pale yellow
val AvgHeartRateColor = Color(0xFFFF4757)   // red
val MaxHeartRateColor = Color(0xFFFF6B81)   // pink-red
val CaloriesPerHourColor = Color(0xFFFFA502)// amber
val ConditionColor = Color(0xFF7BED9F)      // green
val MovesColor = Color(0xFFA29BFE)          // lavender
